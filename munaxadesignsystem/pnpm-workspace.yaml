# Marks this directory as its own pnpm root so installs never resolve against the
# parent Munaxa workspace, and the lockfile lives here. Required for the
# standalone Cloudflare app (matches munaxalanding / munaxademo).
onlyBuiltDependencies:
  - esbuild
  - "@tailwindcss/oxide"
  - workerd
