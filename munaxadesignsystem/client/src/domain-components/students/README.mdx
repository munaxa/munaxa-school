# Students domain components

## Overview and purpose
These components encode students concepts while preserving permission, workflow, notification, accessibility, RTL, and dark-mode contracts.

| Component | Purpose | Variants | States |
|---|---|---|---|
+| StudentCard | Encodes the student card concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentAvatar | Encodes the student avatar concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentProfileHeader | Encodes the student profile header concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentStatusBadge | Encodes the student status badge concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentTimeline | Encodes the student timeline concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentMetrics | Encodes the student metrics concept | default, compact, or actionable | empty-safe and domain-state aware |
| EnrollmentStatus | Encodes the enrollment status concept | default, compact, or actionable | empty-safe and domain-state aware |
| GuardianSummary | Encodes the guardian summary concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentQuickActions | Encodes the student quick actions concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentIdentityPanel | Encodes the student identity panel concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentInfoGrid | Encodes the student info grid concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentContactPanel | Encodes the student contact panel concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentEmergencyContact | Encodes the student emergency contact concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentDocumentsPanel | Encodes the student documents panel concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentTags | Encodes the student tags concept | default, compact, or actionable | empty-safe and domain-state aware |
| StudentSummaryCard | Encodes the student summary card concept | default, compact, or actionable | empty-safe and domain-state aware |

## Usage rules
Use inside approved patterns after permission filtering. Pass localized values and stable states. Keep fetching, authorization, mutation, and transitions outside presentation components.

## Do
- Reuse shared foundations.
- Provide explicit state, scope, and accessible labels.
- Compose loading, empty, denied, error, approval, and read-only states.

## Don’t
- Copy markup or add one-off status colors.
- Hide unauthorized data only in the browser.
- Use cards without workflow purpose.

## Accessibility
Text accompanies status. Controls work by keyboard, have names, preserve focus, and do not rely on hover, color, sound, or motion.

## RTL
Use logical flow and real Arabic. Isolate amounts, phone numbers, IDs, and mixed-direction strings.

## Dark mode
Use semantic tokens only. No neon, glow, glassmorphism, or luminous statuses.

## Examples
Complete compositions under domain-components/examples use only approved APIs.
