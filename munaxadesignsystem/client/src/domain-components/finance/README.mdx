# Finance domain components

## Overview and purpose
These components encode finance concepts while preserving permission, workflow, notification, accessibility, RTL, and dark-mode contracts.

| Component | Purpose | Variants | States |
|---|---|---|---|
+| BalanceCard | Encodes the balance card concept | default, compact, or actionable | empty-safe and domain-state aware |
| InvoiceCard | Encodes the invoice card concept | default, compact, or actionable | empty-safe and domain-state aware |
| PaymentCard | Encodes the payment card concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReceiptCard | Encodes the receipt card concept | default, compact, or actionable | empty-safe and domain-state aware |
| CollectionSummary | Encodes the collection summary concept | default, compact, or actionable | empty-safe and domain-state aware |
| FeeStatusCard | Encodes the fee status card concept | default, compact, or actionable | empty-safe and domain-state aware |
| AgingCard | Encodes the aging card concept | default, compact, or actionable | empty-safe and domain-state aware |
| FinanceMetrics | Encodes the finance metrics concept | default, compact, or actionable | empty-safe and domain-state aware |
| OutstandingBalanceCard | Encodes the outstanding balance card concept | default, compact, or actionable | empty-safe and domain-state aware |
| PaymentMethodCard | Encodes the payment method card concept | default, compact, or actionable | empty-safe and domain-state aware |
| TransactionTimeline | Encodes the transaction timeline concept | default, compact, or actionable | empty-safe and domain-state aware |
| FinanceQuickActions | Encodes the finance quick actions concept | default, compact, or actionable | empty-safe and domain-state aware |
| FinanceStatusBadge | Encodes the finance status badge concept | default, compact, or actionable | empty-safe and domain-state aware |

## Usage rules
Use inside approved patterns after permission filtering. Pass localized values and stable states. Keep fetching, authorization, mutation, and transitions outside presentation components.

## Do
- Reuse shared foundations.
- Provide explicit state, scope, and accessible labels.
- Compose loading, empty, denied, error, approval, and read-only states.

## Don’t
- Copy markup or add one-off status colors.
- Hide unauthorized data only in the browser.
- Use cards without workflow purpose.

## Accessibility
Text accompanies status. Controls work by keyboard, have names, preserve focus, and do not rely on hover, color, sound, or motion.

## RTL
Use logical flow and real Arabic. Isolate amounts, phone numbers, IDs, and mixed-direction strings.

## Dark mode
Use semantic tokens only. No neon, glow, glassmorphism, or luminous statuses.

## Examples
Complete compositions under domain-components/examples use only approved APIs.
