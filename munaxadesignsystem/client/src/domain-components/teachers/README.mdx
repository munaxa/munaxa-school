# Teachers domain components

## Overview and purpose
These components encode teachers concepts while preserving permission, workflow, notification, accessibility, RTL, and dark-mode contracts.

| Component | Purpose | Variants | States |
|---|---|---|---|
+| TeacherCard | Encodes the teacher card concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherProfileHeader | Encodes the teacher profile header concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherStatusBadge | Encodes the teacher status badge concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherScheduleCard | Encodes the teacher schedule card concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherWorkloadCard | Encodes the teacher workload card concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherSummaryCard | Encodes the teacher summary card concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherContactPanel | Encodes the teacher contact panel concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherSubjectsPanel | Encodes the teacher subjects panel concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherAttendanceCard | Encodes the teacher attendance card concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherTimeline | Encodes the teacher timeline concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherQuickActions | Encodes the teacher quick actions concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherMetrics | Encodes the teacher metrics concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherAvatar | Encodes the teacher avatar concept | default, compact, or actionable | empty-safe and domain-state aware |
| TeacherIdentityPanel | Encodes the teacher identity panel concept | default, compact, or actionable | empty-safe and domain-state aware |

## Usage rules
Use inside approved patterns after permission filtering. Pass localized values and stable states. Keep fetching, authorization, mutation, and transitions outside presentation components.

## Do
- Reuse shared foundations.
- Provide explicit state, scope, and accessible labels.
- Compose loading, empty, denied, error, approval, and read-only states.

## Don’t
- Copy markup or add one-off status colors.
- Hide unauthorized data only in the browser.
- Use cards without workflow purpose.

## Accessibility
Text accompanies status. Controls work by keyboard, have names, preserve focus, and do not rely on hover, color, sound, or motion.

## RTL
Use logical flow and real Arabic. Isolate amounts, phone numbers, IDs, and mixed-direction strings.

## Dark mode
Use semantic tokens only. No neon, glow, glassmorphism, or luminous statuses.

## Examples
Complete compositions under domain-components/examples use only approved APIs.
