# Reports domain components

## Overview and purpose
These components encode reports concepts while preserving permission, workflow, notification, accessibility, RTL, and dark-mode contracts.

| Component | Purpose | Variants | States |
|---|---|---|---|
+| ReportCard | Encodes the report card concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportMetric | Encodes the report metric concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportFilterBar | Encodes the report filter bar concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportSummary | Encodes the report summary concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportExportStatus | Encodes the report export status concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportScheduleCard | Encodes the report schedule card concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportCategoryCard | Encodes the report category card concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportInsightCard | Encodes the report insight card concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportTrendCard | Encodes the report trend card concept | default, compact, or actionable | empty-safe and domain-state aware |
| ReportVisualizationWrapper | Encodes the report visualization wrapper concept | default, compact, or actionable | empty-safe and domain-state aware |

## Usage rules
Use inside approved patterns after permission filtering. Pass localized values and stable states. Keep fetching, authorization, mutation, and transitions outside presentation components.

## Do
- Reuse shared foundations.
- Provide explicit state, scope, and accessible labels.
- Compose loading, empty, denied, error, approval, and read-only states.

## Don’t
- Copy markup or add one-off status colors.
- Hide unauthorized data only in the browser.
- Use cards without workflow purpose.

## Accessibility
Text accompanies status. Controls work by keyboard, have names, preserve focus, and do not rely on hover, color, sound, or motion.

## RTL
Use logical flow and real Arabic. Isolate amounts, phone numbers, IDs, and mixed-direction strings.

## Dark mode
Use semantic tokens only. No neon, glow, glassmorphism, or luminous statuses.

## Examples
Complete compositions under domain-components/examples use only approved APIs.
