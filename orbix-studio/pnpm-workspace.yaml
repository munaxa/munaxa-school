# Marks this directory as its own pnpm root so installs never resolve against the
# parent Munaxa workspace, and the lockfile lives here. Keeps the Orbix Studio
# design-system showcase fully standalone.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - workerd
